# 成长打卡隔离与运行接入进度

记录时间：2026-09-08 06:52（Asia/Shanghai）。此文件是进度证据，不是上线验收报告。07:00 完整体验目标尚未完成。

## 已实现并完成本地验证

- 云函数唯一部署目标 `taskCheckinCoreApi`；根配置的 `cloudfunctionRoot` 指向 `dist/deploy/`，不把原有租房函数目录纳入部署。
- 41 个集合统一映射到 `task_checkin_*`，仓储与部署 manifest 使用同一份映射；未新增对租房集合的访问。
- 调用方 AppID 白名单默认拒绝。共享调用校验 `FROM_APPID + FROM_OPENID` 配对，直连使用 `APPID + OPENID`，请求体伪造身份不能替代运行时身份。
- 数据迁移工具支持专属集合导出、校验和、空目标预检、保留文档 ID、文件复制与字节校验、新 fileID 映射、导入后比较；不删除源数据。
- 真实图片上传通过服务端核对上传人、空间、路径、字节数和文件头类型，限制 1 MB；返回业务图片 ID。提交时再次检查有效媒体并事务内建立证据关系。禁止用租房云文件 ID 或客户端自报上传完成冒充本项目图片。
- 重复提交的幂等返回也先验证访问权限。
- 提交详情返回真实文字和图片引用；仅监护人或对应分组审核人员可读；授权通过后申请图片访问链接。
- 家庭初始化、添加孩子、家长首页、孩子今日页、发布任务、孩子提交、审核、种树/采摘、任务列表和愿望已接入业务接口。任务筛选真实改变显示列表，愿望使用当前选中孩子与实际愿望/果实 ID。
- 独立 CommonJS 云函数构建与微信页面产物构建；共享主题依赖已打入小程序内部，不引用包外 `src/`。
- 顶部自定义栏根据胶囊与状态栏预留空间，底部预留安全区；视觉效果仍待设备验证。

## 本次新增修复与验证

- 教师发布页接入本机构有效模板，过滤其他机构、家庭及已归档模板；学科可选择，模板参数实际传入发布接口，日期不沿用旧模板日期。页面操作→真实 Core API→任务/孩子任务记录的本地集成测试通过。
- 补齐退组后的审核队列隔离：即使孩子仍在同机构另一分组，旧分组也不能继续读取其提交摘要；原有详情/图片拒绝访问不能代替队列过滤。
- 家庭设置、家长果园、孩子资料与分组、加入邀请、教师分组/任务/审核等页面已改为业务接口数据；仍需真实环境端到端验收。
- 退组后分组老师不能继续用已知记录 ID 获取孩子提交和图片的新访问链接；已签发的临时 URL 不能据此声称即时失效。
- 审核页显示后端授权返回的孩子标识，不能用前端路由参数冒充审核对象。
- 孩子果园去除初始假阳光和假果实；加载失败不显示旧数据，不允许种树，并提供重试。
- 真实 `wx-server-sdk@4.0.2` 检查发现文档引用不存在 `create()`，原假 SDK 测试遗漏了这个阻断云端插入的问题。已改为集合 `add` 并显式保留 `_id`。新增使用真实 SDK、仅替换网络传输的测试，验证事务插入、ID 保留、重复审计记录拒绝和回滚调用；不是云端事务验收。

## 本地质量门（最新复核至 08:04）

- `npm run typecheck`：PASS。
- `npm run lint`：PASS（249 个文件，无修复或提示）。
- 测试用例：08:00 再次 PASS（73 个文件，252 项测试）。
- `npm run test:coverage`：FAIL，应用层分支覆盖率 84.53%，低于原定 85% 门槛；未降低阈值。
- `npm run format:check`：07:37 检查失败。已格式化教师审核页；开发者工具生成的 `project.private.config.json` 缺末尾换行未改动，不能声称全量格式门通过。
- 部署产物运行测试：所有注册页面可作为 CommonJS 加载，无页面包外依赖；不是微信渲染或触控验收。
- `npm run build:deploy` 及 `node scripts/build-deployment.mjs --require-config`：PASS，`cloud configured: true`。该标记只表示本机私有配置非空；不表示环境归属、关联关系或云端可用性已验证。
- `git diff --check`：PASS。

## 真实环境证据与阻塞

- 本机曾从微信开发者工具 CLI 查询到 `cloud1-d2gxpfha4e8de0a0d`，但 2026-09-07 已在“握个手”腾讯云账号的控制台核对：该账号当前仅显示 `zufang-9g5z3mbf127882aa`（租房微信环境）与 `lowcode-9g33aojw1fe0b35d`（微搭环境）。`cloud1-d2gxpfha4e8de0a0d` 的归属未核实，不能作为打卡项目部署目标。
- 07:28 官方 CLI 再次查询 `taskCheckinCoreApi` 返回 `ResourceNotFound.Function`，说明该名称当前尚未部署。
- 已尝试两次仅部署 `taskCheckinCoreApi`，均报告 `success: false`、`getCloudAPISignedHeader failed`、`ret: 41002`、`system error.`。首轮失败后函数查询仍为不存在；第二轮失败后的远端状态尚未复核，不能声称部署成功。
- 本轮未建集合、修改权限、上传照片或运行迁移，未针对租房资源执行写入。
- 用户已明确确认 `wx7f63176424216ee8` 为新打卡小程序 AppID；已写入被 Git 忽略的 `.task-checkin.local.json`。实际采用直连还是资源共享仍需实测，不能仅凭环境列表推断。
- 腾讯云二次登录验证已通过；切换至“握个手”主账号后，已核实上述两个现有环境。用户尚未决定是否复用租房环境；在明确授权且完成最小权限配置前，不部署、不建库、不修改任何现有资源。
- 07:25 微信开发者工具和截图已可访问，锁屏不再是当前阻塞；模拟器可见角色入口页，不能据此认定云端流程可用。控制台仍有 TDesign 字体加载失败；顶部/Tab/业务流程尚未完成实际交互验收。
- 微信公众平台对应页面先前被浏览器安全策略禁止访问；不得换工具或请求方式绕过限制。

## 仍需完成，不能省略或用静态演示代替

1. 页面接入尚未得到完整产品验收。教师模板选择已接通，但机构授权初始化等入口仍需补全；果树展示仍有苹果素材固定映射，需核对所有品种和成长阶段；应用层覆盖率门尚未通过。
2. 教师/机构身份初始化和授权链路需要真实配置，不能让任意首次注册者自动成为平台管理员。
3. OCR 提供方仍未配置；当前明确返回未配置错误。正式导出文件也未接通，只有导出申请业务。
4. 核实登录账号拥有目标环境并解决部署签名错误，部署唯一新函数及其 `ALLOWED_CALLER_APPIDS`（构建 manifest 不会自动设置远端环境变量）。按 manifest 创建 41 个专属集合和索引，只修改这些集合规则。
5. 共享环境存储权限必须明确作用于本路径；不能覆盖全局规则影响租房，也不能让儿童照片继承公开读取。尚未核实前禁止上传真实儿童作业。
6. 在真实环境验证 SDK 数据库事务、唯一索引、图片访问和权限拒绝；本地仓储模拟不等于 CloudBase 验收。
7. 微信开发者工具/真机验证顶部遮挡、全部 Tab、冷启动、弱网、刷新持久化，以及创建家庭→发布→孩子提交→审核→阳光→采摘→愿望全流程。
8. 真实迁移演练尚未运行，需验证数据和照片在独立目标环境可用后再切换。
9. 云函数依赖安装曾报告 6 项上游漏洞（1 moderate、5 high），仍需具体风险评估，未执行破坏性 audit force 升级。

## 隔离范围

同环境命名前缀、调用方校验和客户端规则构成逻辑隔离，不是抵御同环境其他管理员/高权限云函数的强隔离。若需要互不影响的管理员权限与资源边界，必须迁移至独立环境，并配置相应最小权限。完整步骤见 `docs/runbooks/cloudbase-release.md`。
